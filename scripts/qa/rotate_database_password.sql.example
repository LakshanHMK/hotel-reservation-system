-- MANUAL DBA ACTION ONLY. Do not run the placeholder unchanged.
-- Local read-only verification confirmed CURRENT_USER() = lankastay_app@localhost.
-- Use an administrative MySQL session, generate a new random secret privately,
-- and replace the placeholder below. Never commit the replacement.
ALTER USER 'lankastay_app'@'localhost'
    IDENTIFIED BY 'REPLACE_WITH_NEW_RANDOM_PASSWORD';
-- No FLUSH PRIVILEGES is required after ALTER USER.
-- Update ignored .env DB_PASSWORD and the deployment secret store together.
-- Restart the backend, then confirm SELECT 1 using the new environment value.
