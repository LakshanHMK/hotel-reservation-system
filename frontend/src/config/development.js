// This preview is never real authentication and is excluded from production builds.
export const ENABLE_MANAGEMENT_PREVIEW = import.meta.env.DEV

export const MANAGEMENT_PREVIEW_MODE = 'managementPreviewMode'
