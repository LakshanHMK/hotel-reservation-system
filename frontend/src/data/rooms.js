// Room data is loaded from the backend API. No mock/seed data is included
// to prevent a flash of stale rooms before the real backend response arrives.

export const rooms = [];

export default rooms;
